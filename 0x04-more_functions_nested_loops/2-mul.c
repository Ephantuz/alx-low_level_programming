#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies two integers
 *@a: The first number to multiply
 *@b: The second number to multiply
 * Return: "a" and "b"
 */
int mul(int a, int b)
{
return (a * b);
}
